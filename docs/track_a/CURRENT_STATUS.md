# Track A Current Status

Track A is the primary active experiment.

Current stage:

- repository setup;
- protocol preparation;
- no model implementation;
- no event prediction;
- no event-result inspection.

Models:

- CNP with uniform mean aggregation;
- AdaCNP with target-conditioned adaptive aggregation.

The intended controlled difference is the aggregation mechanism. Context sets,
encoders, decoders, partitions, normalization, optimization, and evaluation
conditions must otherwise be paired.
